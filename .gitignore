# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build and test output
/.next/
/out/
/build/
/coverage/
*.tsbuildinfo
next-env.d.ts

# Local configuration and secrets
.env*
!.env.example
.vercel/
!.vercel/repo.json
*.pem

# Local/debug files
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev.log
server.log
local-*
.claude
.z-ai-config
prompt
test

# Local databases, backups, uploads, and generated archives
*.db
*.db-journal
/db/
/data/
/backups/
/backup-setup-*/
/backup-final-install-*/
/public/uploads/*
!/public/uploads/.gitkeep
npm-audit-*.json

# Deployment binaries and extracted package contents
/usr/
*.deb
*.tar.gz
debian-binary

# Social media credentials (contains live passwords — never commit)
/secrets/

# Local-only launch helpers and logs
cloudflared.log
cloudflared-custom.log
cloudflared-*.log
proximity-vps-update-and-launch.sh

# Browser control (agent skill) artifacts
.browser-profile/
.browser-state.json
shots/

# Agent tooling
.omo/
